import React, { Component } from 'react';

export default class Test extends Component {
    static defaultProps = {};
    props: {};
    state: void;

    render() {
        const { } = this.props;

        return (
            <div>
                Hi there!
            </div>
        );
    }
}

const STYLES = {};
